# 08_replication_ha

Replication, Group Replication, binary log generation, lag, and HA posture.

Scripts in this area: `4`.

## Usage

Run with the mysql client, for example: `mysql -u root -p < 08_replication_ha/<script>.sql`.

## Scripts

- `01_primary_replication_status.sql`
- `02_standby_replay_status.sql`
- `03_replication_slots_health.sql`
- `04_binlog_generation_rate.sql`
