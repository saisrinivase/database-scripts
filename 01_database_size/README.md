# 01_database_size

Schema, tablespace, table, and storage size diagnostics.

Scripts in this area: `3`.

## Usage

Run with the mysql client, for example: `mysql -u root -p < 01_database_size/<script>.sql`.

## Scripts

- `01_databases_size.sql`
- `02_current_database_size_breakdown.sql`
- `03_tablespaces_size.sql`
