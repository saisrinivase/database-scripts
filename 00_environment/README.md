# 00_environment

MySQL server, version, plugin, component, and schema inventory.

Scripts in this area: `3`.

## Usage

Run with the mysql client, for example: `mysql -u root -p < 00_environment/<script>.sql`.

## Scripts

- `01_server_instance_overview.sql`
- `02_plugins_components_installed.sql`
- `03_database_catalog_overview.sql`
